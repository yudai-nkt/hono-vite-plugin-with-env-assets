{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preserve": "npm run build",
    "serve": "wrangler pages dev dist",
    "start": "wrangler pages dev -- vite"
  },
  "license": "CC0",
  "dependencies": {
    "hono": "^3.9.0"
  },
  "devDependencies": {
    "@hono/vite-cloudflare-pages": "^0.0.2",
    "@hono/vite-dev-server": "^0.0.12",
    "vite": "^4.5.0",
    "wrangler": "^3.15.0"
  }
}
